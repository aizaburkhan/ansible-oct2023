- name: Install other packages
  yum: 
   name: "{{ packages }}" #created a variable
  vars: 
    packages: 
    - wget
    - unzip
    - epel-release
    - mysql
    - https://rpms.remirepo.net/enterprise/remi-release-7.rpm
